import 'package:flutter/material.dart';

class ExamanView extends StatelessWidget {
  const ExamanView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Examns View'),
    );
  }
}