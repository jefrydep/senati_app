import 'package:flutter/material.dart';

class TriajeView extends StatelessWidget {
  const TriajeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('triaje Screen'),
    );
  }
}